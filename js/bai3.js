/**
 * Khối 1: Input
 * Nhập giá 1 usd hiện tại
 * oneUsd
 * inputUsd
 * 
 * Khối 2: 
 * Tính số tiền đã quy đổi
 * vnd = oneUsd * inputUsd
 * 
 * Khối 3: Output
 * Số tiền đã quy đổi
 */

// Tạo và gán giá trị
var oneUsd = 23500;
var inputUsd = 2;
var vnd = 0;

// Tính số tiền đã quy đổi
vnd = oneUsd * inputUsd;
console.log("Số tiền đã quy đổi là: " + vnd);
